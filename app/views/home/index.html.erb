<%= link_to '+ 添加新任务', new_post_path, class:"btn btn-primary"%>
<%= render 'table' %>
<%= will_paginate @post, {
  :renderer => 'BootstrapPaginationHelper::LinkRenderer', 
  :previous_label => "上一页",
  :next_label => '下一页',
  :class => 'pagination pagination-small pagination-centered'
}%>

